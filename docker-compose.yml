services:
  orsk-bot:
    build: .
    container_name: orsk-bot
    restart: unless-stopped
    environment:
      TZ: Asia/Yekaterinburg
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"